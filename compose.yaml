services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: jinjuwiki
      POSTGRES_USER: jinjuwiki
      POSTGRES_PASSWORD: jinjuwiki
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
